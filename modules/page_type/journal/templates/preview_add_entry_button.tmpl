<button class="add-entry">Add new entry</button>
<ul class="menu">
	{{identifierContext=start;name=journal}}
	<li data-journal-id="{{journal.id}}">{{journal.name}}</li>
	{{identifierContext=end;name=journal}}
</ul>