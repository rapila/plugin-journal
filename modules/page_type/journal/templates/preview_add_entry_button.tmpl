<div class="add_entry_wrapper">
	<button class="add-entry ui-button ui-widget ui-state-default ui-corner-all"{{journal_id_data}}>{{writeString=journal_entry.preview.add_new_entry}}</button>
	<ul class="ui-menu" style="display:none;">
		{{identifierContext=start;name=journal}}
		<li data-journal-id="{{journal.id}}"><a>{{journal.name}}</a></li>
		{{identifierContext=end;name=journal}}
	</ul>
</div>
